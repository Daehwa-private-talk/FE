const colors = {
  black: '#333333',
  dark: '#183f3c',
  violet: '#ad588f',
  purple: '#555188',
  red: '#ec6e69',
  yellow: '#f1aa3c',
};

const spacing = (number: number) => `${number * 2}px`;

const theme = {
  colors,
  spacing,
};

export default theme;
